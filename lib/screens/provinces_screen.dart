import 'package:flutter/material.dart';

class ProvincesFiltterScreen extends StatelessWidget {
  const ProvincesFiltterScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Noob"),
    );
  }
}
